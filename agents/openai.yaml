interface:
  display_name: "ShareXtract"
  short_description: "Extract public shared content with protocol-first fallbacks"
  default_prompt: "Use $sharextract to extract this public share URL with the safest highest-fidelity method, preserve provenance, and report any fallback or uncertainty."
